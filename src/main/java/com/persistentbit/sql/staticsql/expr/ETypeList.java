package com.persistentbit.sql.staticsql.expr;

/**
 * An {@link Expr} type representing a Sql list of all the same types.<br>
 * @author Peter Muys
 * @since 14/10/16
 */
public interface ETypeList<T> extends Expr<T>{
}
